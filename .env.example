# Convex deployment URL — leave blank to disable the backup layer (app
# falls back to localStorage-only). Get this URL by running `npx convex dev`
# from the repo root the first time; Convex prints/saves the deployment URL
# and provisions the schema in `convex/`. See docs/PERSISTENCE.md.
VITE_CONVEX_URL=
